#############################################
# BACKEND ONLY ENV EXAMPLE (Alternative)     #
#############################################
# Use this only if you prefer a separate backend/.env
# Otherwise keep everything in root .env

NODE_ENV=development
PORT=3001

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=musda

JWT_SECRET=ubah_ini_dengan_string_random_panjang
FRONTEND_URL=http://localhost:5173

EMAIL_ENABLED=true
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=yourgmail@example.com
SMTP_PASS=your_app_password
SMTP_FROM="MUSDA II <yourgmail@example.com>"

WILAYAH_API_BASE=https://www.emsifa.com/api-wilayah-indonesia/api

# Uncomment if you add file size or rate limit logic later
# MAX_UPLOAD_MB=10
# LOG_LEVEL=info

#############################################
# RAILWAY DEPLOY NOTES                      #
#############################################
# Jika memakai plugin MySQL Railway, variabel di bawah otomatis tersedia:
# MYSQLHOST, MYSQLUSER, MYSQLPASSWORD, MYSQLDATABASE, MYSQLPORT
# File db.js sudah mendukung fallback ke variabel tsb.
# Jadi DB_HOST/DB_USER/DB_PASSWORD/DB_NAME boleh dikosongkan.

# WAJIB di Railway (sesuaikan):
# NODE_ENV=production
# PORT=3001
# JWT_SECRET=string_random_panjang
# FRONTEND_URL=https://domain-netlify.app
# EMAIL_ENABLED=true
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=yourgmail@example.com
# SMTP_PASS=app_password
# SMTP_FROM="MUSDA II <no-reply@domain.com>"
# WILAYAH_API_BASE=https://www.emsifa.com/api-wilayah-indonesia/api

# Opsional:
# CORS_WHITELIST=https://domain-netlify.app,https://sub.example.com
# LOG_LEVEL=info
# MAX_UPLOAD_MB=10


